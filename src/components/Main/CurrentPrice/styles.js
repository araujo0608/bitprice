import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    containerText:{
        marginTop: 60
    },
    mainText:{
        fontSize: 32,
        color: 'white',
        fontWeight: 'bold'
    },
    subText:{
        fontSize: 12,
        color: 'white',
        fontStyle: 'italic',
        left: 25
    }
}); 

export default styles;